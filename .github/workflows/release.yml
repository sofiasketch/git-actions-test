# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: .NET release

on:
  workflow_dispatch

env:
  NuGetDirectory: ${{ github.workspace}}/nuget

jobs:
  build:
    name: release
    
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - uses: actions/checkout@v3
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 6.0.x
        
    - name: Restore dependencies
      run: dotnet restore
      
    - name: Build
      run: dotnet build --no-restore
      
    - name: Test
      run: dotnet test --no-build --verbosity normal
      
    - name: Pack
      run: dotnet pack --configuration Release --output ${{ env.NuGetDirectory }}
      
    - name: Upload Artifacts
    - uses: actions/upload-artifact@v3
      with:
        name: nuget
        if-no-files-found: error
        retention-days: 7
        path: ${{ env.NuGetDirectory }}/*.nupkg
      
    - name: GetVersion
      id: package_version
      uses: KageKirin/get-csproj-version@v1.0.0
      with:
        file: PowerCollections/PowerCollections.csproj
      
    - name: Push
      run: dotnet nuget push "**/*.nupkg" --skip-duplicate --api-key ${{ secrets.TOKEN  }} --source "https://nuget.pkg.github.com/sofiasketch/index.json"
      
    - uses: actions/download-artifact@v3
      with:
        name: nuget
        path: ${{ env.NuGetDirectory }}
      
    - name: Create Release
      uses: ncipollo/release-action@v1.12.0
      with:
        artifacts: "${{ env.NuGetDirectory }}/sofiasketch.PowerCollections.{{ steps.package_version.outputs.version }}.nupkg"
        tag: "v${{ steps.package_version.outputs.version }}"

